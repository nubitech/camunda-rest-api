/*
 * Camunda Platform REST API
 * OpenApi Spec for Camunda Platform REST API.
 *
 * OpenAPI spec version: 7.20.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.46
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';

/**
 * The HistoricActivityStatisticsDto model module.
 * @module model/HistoricActivityStatisticsDto
 * @version 7.20.0
 */
export class HistoricActivityStatisticsDto {
  /**
   * Constructs a new <code>HistoricActivityStatisticsDto</code>.
   * @alias module:model/HistoricActivityStatisticsDto
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>HistoricActivityStatisticsDto</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/HistoricActivityStatisticsDto} obj Optional instance to populate.
   * @return {module:model/HistoricActivityStatisticsDto} The populated <code>HistoricActivityStatisticsDto</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new HistoricActivityStatisticsDto();
      if (data.hasOwnProperty('id'))
        obj.id = ApiClient.convertToType(data['id'], 'String');
      if (data.hasOwnProperty('instances'))
        obj.instances = ApiClient.convertToType(data['instances'], 'Number');
      if (data.hasOwnProperty('canceled'))
        obj.canceled = ApiClient.convertToType(data['canceled'], 'Number');
      if (data.hasOwnProperty('finished'))
        obj.finished = ApiClient.convertToType(data['finished'], 'Number');
      if (data.hasOwnProperty('completeScope'))
        obj.completeScope = ApiClient.convertToType(data['completeScope'], 'Number');
      if (data.hasOwnProperty('openIncidents'))
        obj.openIncidents = ApiClient.convertToType(data['openIncidents'], 'Number');
      if (data.hasOwnProperty('resolvedIncidents'))
        obj.resolvedIncidents = ApiClient.convertToType(data['resolvedIncidents'], 'Number');
      if (data.hasOwnProperty('deletedIncidents'))
        obj.deletedIncidents = ApiClient.convertToType(data['deletedIncidents'], 'Number');
    }
    return obj;
  }
}

/**
 * The id of the activity the results are aggregated for.
 * @member {String} id
 */
HistoricActivityStatisticsDto.prototype.id = undefined;

/**
 * The total number of all running instances of the activity.
 * @member {Number} instances
 */
HistoricActivityStatisticsDto.prototype.instances = undefined;

/**
 * The total number of all canceled instances of the activity. **Note:** Will be `0` (not `null`), if canceled activity instances were excluded.
 * @member {Number} canceled
 */
HistoricActivityStatisticsDto.prototype.canceled = undefined;

/**
 * The total number of all finished instances of the activity. **Note:** Will be `0` (not `null`), if finished activity instances were excluded.
 * @member {Number} finished
 */
HistoricActivityStatisticsDto.prototype.finished = undefined;

/**
 * The total number of all instances which completed a scope of the activity. **Note:** Will be `0` (not `null`), if activity instances which completed a scope were excluded.
 * @member {Number} completeScope
 */
HistoricActivityStatisticsDto.prototype.completeScope = undefined;

/**
 * The total number of open incidents for the activity. **Note:** Will be `0` (not `null`), if `incidents` is set to `false`.
 * @member {Number} openIncidents
 */
HistoricActivityStatisticsDto.prototype.openIncidents = undefined;

/**
 * The total number of resolved incidents for the activity. **Note:** Will be `0` (not `null`), if `incidents` is set to `false`.
 * @member {Number} resolvedIncidents
 */
HistoricActivityStatisticsDto.prototype.resolvedIncidents = undefined;

/**
 * The total number of deleted incidents for the activity. **Note:** Will be `0` (not `null`), if `incidents` is set to `false`.
 * @member {Number} deletedIncidents
 */
HistoricActivityStatisticsDto.prototype.deletedIncidents = undefined;

