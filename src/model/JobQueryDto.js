/*
 * Camunda Platform REST API
 * OpenApi Spec for Camunda Platform REST API.
 *
 * OpenAPI spec version: 7.20.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.46
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {JobConditionQueryParameterDto} from './JobConditionQueryParameterDto';
import {JobQueryDtoSorting} from './JobQueryDtoSorting';

/**
 * The JobQueryDto model module.
 * @module model/JobQueryDto
 * @version 7.20.0
 */
export class JobQueryDto {
  /**
   * Constructs a new <code>JobQueryDto</code>.
   * A Job instance query which defines a list of Job instances
   * @alias module:model/JobQueryDto
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>JobQueryDto</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/JobQueryDto} obj Optional instance to populate.
   * @return {module:model/JobQueryDto} The populated <code>JobQueryDto</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new JobQueryDto();
      if (data.hasOwnProperty('jobId'))
        obj.jobId = ApiClient.convertToType(data['jobId'], 'String');
      if (data.hasOwnProperty('jobIds'))
        obj.jobIds = ApiClient.convertToType(data['jobIds'], ['String']);
      if (data.hasOwnProperty('jobDefinitionId'))
        obj.jobDefinitionId = ApiClient.convertToType(data['jobDefinitionId'], 'String');
      if (data.hasOwnProperty('processInstanceId'))
        obj.processInstanceId = ApiClient.convertToType(data['processInstanceId'], 'String');
      if (data.hasOwnProperty('processInstanceIds'))
        obj.processInstanceIds = ApiClient.convertToType(data['processInstanceIds'], ['String']);
      if (data.hasOwnProperty('executionId'))
        obj.executionId = ApiClient.convertToType(data['executionId'], 'String');
      if (data.hasOwnProperty('processDefinitionId'))
        obj.processDefinitionId = ApiClient.convertToType(data['processDefinitionId'], 'String');
      if (data.hasOwnProperty('processDefinitionKey'))
        obj.processDefinitionKey = ApiClient.convertToType(data['processDefinitionKey'], 'String');
      if (data.hasOwnProperty('activityId'))
        obj.activityId = ApiClient.convertToType(data['activityId'], 'String');
      if (data.hasOwnProperty('withRetriesLeft'))
        obj.withRetriesLeft = ApiClient.convertToType(data['withRetriesLeft'], 'Boolean');
      if (data.hasOwnProperty('executable'))
        obj.executable = ApiClient.convertToType(data['executable'], 'Boolean');
      if (data.hasOwnProperty('timers'))
        obj.timers = ApiClient.convertToType(data['timers'], 'Boolean');
      if (data.hasOwnProperty('messages'))
        obj.messages = ApiClient.convertToType(data['messages'], 'Boolean');
      if (data.hasOwnProperty('dueDates'))
        obj.dueDates = ApiClient.convertToType(data['dueDates'], [JobConditionQueryParameterDto]);
      if (data.hasOwnProperty('createTimes'))
        obj.createTimes = ApiClient.convertToType(data['createTimes'], [JobConditionQueryParameterDto]);
      if (data.hasOwnProperty('withException'))
        obj.withException = ApiClient.convertToType(data['withException'], 'Boolean');
      if (data.hasOwnProperty('exceptionMessage'))
        obj.exceptionMessage = ApiClient.convertToType(data['exceptionMessage'], 'String');
      if (data.hasOwnProperty('failedActivityId'))
        obj.failedActivityId = ApiClient.convertToType(data['failedActivityId'], 'String');
      if (data.hasOwnProperty('noRetriesLeft'))
        obj.noRetriesLeft = ApiClient.convertToType(data['noRetriesLeft'], 'Boolean');
      if (data.hasOwnProperty('active'))
        obj.active = ApiClient.convertToType(data['active'], 'Boolean');
      if (data.hasOwnProperty('suspended'))
        obj.suspended = ApiClient.convertToType(data['suspended'], 'Boolean');
      if (data.hasOwnProperty('priorityLowerThanOrEquals'))
        obj.priorityLowerThanOrEquals = ApiClient.convertToType(data['priorityLowerThanOrEquals'], 'Number');
      if (data.hasOwnProperty('priorityHigherThanOrEquals'))
        obj.priorityHigherThanOrEquals = ApiClient.convertToType(data['priorityHigherThanOrEquals'], 'Number');
      if (data.hasOwnProperty('tenantIdIn'))
        obj.tenantIdIn = ApiClient.convertToType(data['tenantIdIn'], ['String']);
      if (data.hasOwnProperty('withoutTenantId'))
        obj.withoutTenantId = ApiClient.convertToType(data['withoutTenantId'], 'Boolean');
      if (data.hasOwnProperty('includeJobsWithoutTenantId'))
        obj.includeJobsWithoutTenantId = ApiClient.convertToType(data['includeJobsWithoutTenantId'], 'Boolean');
      if (data.hasOwnProperty('sorting'))
        obj.sorting = ApiClient.convertToType(data['sorting'], [JobQueryDtoSorting]);
    }
    return obj;
  }
}

/**
 * Filter by job id.
 * @member {String} jobId
 */
JobQueryDto.prototype.jobId = undefined;

/**
 * Filter by a  list of job ids.
 * @member {Array.<String>} jobIds
 */
JobQueryDto.prototype.jobIds = undefined;

/**
 * Only select jobs which exist for the given job definition.
 * @member {String} jobDefinitionId
 */
JobQueryDto.prototype.jobDefinitionId = undefined;

/**
 * Only select jobs which exist for the given process instance.
 * @member {String} processInstanceId
 */
JobQueryDto.prototype.processInstanceId = undefined;

/**
 * Only select jobs which exist for the given  list of process instance ids.
 * @member {Array.<String>} processInstanceIds
 */
JobQueryDto.prototype.processInstanceIds = undefined;

/**
 * Only select jobs which exist for the given execution.
 * @member {String} executionId
 */
JobQueryDto.prototype.executionId = undefined;

/**
 * Filter by the id of the process definition the jobs run on.
 * @member {String} processDefinitionId
 */
JobQueryDto.prototype.processDefinitionId = undefined;

/**
 * Filter by the key of the process definition the jobs run on.
 * @member {String} processDefinitionKey
 */
JobQueryDto.prototype.processDefinitionKey = undefined;

/**
 * Only select jobs which exist for an activity with the given id.
 * @member {String} activityId
 */
JobQueryDto.prototype.activityId = undefined;

/**
 * Only select jobs which have retries left. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} withRetriesLeft
 */
JobQueryDto.prototype.withRetriesLeft = undefined;

/**
 * Only select jobs which are executable, i.e., retries > 0 and due date is `null` or due date is in the past. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} executable
 */
JobQueryDto.prototype.executable = undefined;

/**
 * Only select jobs that are timers. Cannot be used together with `messages`. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} timers
 */
JobQueryDto.prototype.timers = undefined;

/**
 * Only select jobs that are messages. Cannot be used together with `timers`. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} messages
 */
JobQueryDto.prototype.messages = undefined;

/**
 * Only select jobs where the due date is lower or higher than the given date. 
 * @member {Array.<module:model/JobConditionQueryParameterDto>} dueDates
 */
JobQueryDto.prototype.dueDates = undefined;

/**
 * Only select jobs created before or after the given date. 
 * @member {Array.<module:model/JobConditionQueryParameterDto>} createTimes
 */
JobQueryDto.prototype.createTimes = undefined;

/**
 * Only select jobs that failed due to an exception. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} withException
 */
JobQueryDto.prototype.withException = undefined;

/**
 * Only select jobs that failed due to an exception with the given message.
 * @member {String} exceptionMessage
 */
JobQueryDto.prototype.exceptionMessage = undefined;

/**
 * Only select jobs that failed due to an exception at an activity with the given id.
 * @member {String} failedActivityId
 */
JobQueryDto.prototype.failedActivityId = undefined;

/**
 * Only select jobs which have no retries left. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} noRetriesLeft
 */
JobQueryDto.prototype.noRetriesLeft = undefined;

/**
 * Only include active jobs. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} active
 */
JobQueryDto.prototype.active = undefined;

/**
 * Only include suspended jobs. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} suspended
 */
JobQueryDto.prototype.suspended = undefined;

/**
 * Only include jobs with a priority lower than or equal to the given value. Value must be a valid `long` value.
 * @member {Number} priorityLowerThanOrEquals
 */
JobQueryDto.prototype.priorityLowerThanOrEquals = undefined;

/**
 * Only include jobs with a priority higher than or equal to the given value. Value must be a valid `long` value.
 * @member {Number} priorityHigherThanOrEquals
 */
JobQueryDto.prototype.priorityHigherThanOrEquals = undefined;

/**
 * Only include jobs which belong to one of the passed  tenant ids.
 * @member {Array.<String>} tenantIdIn
 */
JobQueryDto.prototype.tenantIdIn = undefined;

/**
 * Only include jobs which belong to no tenant. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} withoutTenantId
 */
JobQueryDto.prototype.withoutTenantId = undefined;

/**
 * Include jobs which belong to no tenant. Can be used in combination with `tenantIdIn`. Value may only be `true`, as `false` is the default behavior.
 * @member {Boolean} includeJobsWithoutTenantId
 */
JobQueryDto.prototype.includeJobsWithoutTenantId = undefined;

/**
 * An array of criteria to sort the result by. Each element of the array is                        an object that specifies one ordering. The position in the array                        identifies the rank of an ordering, i.e., whether it is primary, secondary,                        etc. Does not have an effect for the `count` endpoint.
 * @member {Array.<module:model/JobQueryDtoSorting>} sorting
 */
JobQueryDto.prototype.sorting = undefined;

