/*
 * Camunda Platform REST API
 * OpenApi Spec for Camunda Platform REST API.
 *
 * OpenAPI spec version: 7.20.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.46
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from "../ApiClient";
import {ActivityStatisticsResultDto} from '../model/ActivityStatisticsResultDto';
import {AuthorizationExceptionDto} from '../model/AuthorizationExceptionDto';
import {BatchDto} from '../model/BatchDto';
import {CalledProcessDefinitionDto} from '../model/CalledProcessDefinitionDto';
import {CountResultDto} from '../model/CountResultDto';
import {ExceptionDto} from '../model/ExceptionDto';
import {FormDto} from '../model/FormDto';
import {HistoryTimeToLiveDto} from '../model/HistoryTimeToLiveDto';
import {ProcessDefinitionDiagramDto} from '../model/ProcessDefinitionDiagramDto';
import {ProcessDefinitionDto} from '../model/ProcessDefinitionDto';
import {ProcessDefinitionStatisticsResultDto} from '../model/ProcessDefinitionStatisticsResultDto';
import {ProcessDefinitionSuspensionStateDto} from '../model/ProcessDefinitionSuspensionStateDto';
import {ProcessInstanceDto} from '../model/ProcessInstanceDto';
import {ProcessInstanceWithVariablesDto} from '../model/ProcessInstanceWithVariablesDto';
import {RestartProcessInstanceDto} from '../model/RestartProcessInstanceDto';
import {StartProcessInstanceDto} from '../model/StartProcessInstanceDto';
import {StartProcessInstanceFormDto} from '../model/StartProcessInstanceFormDto';
import {VariableValueDto} from '../model/VariableValueDto';

/**
* ProcessDefinition service.
* @module api/ProcessDefinitionApi
* @version 7.20.0
*/
export class ProcessDefinitionApi {

    /**
    * Constructs a new ProcessDefinitionApi. 
    * @alias module:api/ProcessDefinitionApi
    * @class
    * @param {module:ApiClient} [apiClient] Optional API client implementation to use,
    * default to {@link module:ApiClient#instanc
    e} if unspecified.
    */
    constructor(apiClient) {
        this.apiClient = apiClient || ApiClient.instance;
    }

    /**
     * Callback function to receive the result of the deleteProcessDefinition operation.
     * @callback moduleapi/ProcessDefinitionApi~deleteProcessDefinitionCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete
     * Deletes a running process instance by id.
     * @param {String} id The id of the process definition to be deleted.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.cascade &#x60;true&#x60;, if all process instances, historic process instances and jobs for this process definition should be deleted.
     * @param {Boolean} opts.skipCustomListeners &#x60;true&#x60;, if only the built-in ExecutionListeners should be notified with the end event. (default to <.>)
     * @param {Boolean} opts.skipIoMappings A boolean value to control whether input/output mappings should be executed during deletion. &#x60;true&#x60;, if input/output mappings should not be invoked. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~deleteProcessDefinitionCallback} callback The callback function, accepting three arguments: error, data, response
     */
    deleteProcessDefinition(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling deleteProcessDefinition");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'cascade': opts['cascade'],'skipCustomListeners': opts['skipCustomListeners'],'skipIoMappings': opts['skipIoMappings']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/{id}', 'DELETE',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the deleteProcessDefinitionsByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~deleteProcessDefinitionsByKeyCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete By Key
     * Deletes process definitions by a given key which belong to no tenant id.
     * @param {String} key The key of the process definitions to be deleted.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.cascade &#x60;true&#x60;, if all process instances, historic process instances and jobs for this process definition should be deleted.
     * @param {Boolean} opts.skipCustomListeners &#x60;true&#x60;, if only the built-in ExecutionListeners should be notified with the end event. (default to <.>)
     * @param {Boolean} opts.skipIoMappings A boolean value to control whether input/output mappings should be executed during deletion. &#x60;true&#x60;, if input/output mappings should not be invoked. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~deleteProcessDefinitionsByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     */
    deleteProcessDefinitionsByKey(key, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling deleteProcessDefinitionsByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        'cascade': opts['cascade'],'skipCustomListeners': opts['skipCustomListeners'],'skipIoMappings': opts['skipIoMappings']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}', 'DELETE',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the deleteProcessDefinitionsByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~deleteProcessDefinitionsByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Delete By Key
     * Deletes process definitions by a given key and which belong to a tenant id.
     * @param {String} key The key of the process definitions to be deleted.
     * @param {String} tenantId The id of the tenant the process definitions belong to.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.cascade &#x60;true&#x60;, if all process instances, historic process instances and jobs for this process definition should be deleted.
     * @param {Boolean} opts.skipCustomListeners &#x60;true&#x60;, if only the built-in ExecutionListeners should be notified with the end event. (default to <.>)
     * @param {Boolean} opts.skipIoMappings A boolean value to control whether input/output mappings should be executed during deletion. &#x60;true&#x60;, if input/output mappings should not be invoked. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~deleteProcessDefinitionsByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     */
    deleteProcessDefinitionsByKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling deleteProcessDefinitionsByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling deleteProcessDefinitionsByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        'cascade': opts['cascade'],'skipCustomListeners': opts['skipCustomListeners'],'skipIoMappings': opts['skipIoMappings']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}', 'DELETE',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getActivityStatistics operation.
     * @callback moduleapi/ProcessDefinitionApi~getActivityStatisticsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ActivityStatisticsResultDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Activity Instance Statistics
     * Retrieves runtime statistics of a given process definition, grouped by activities. These statistics include the number of running activity instances, optionally the number of failed jobs and also optionally the number of incidents either grouped by incident types or for a specific incident type. **Note**: This does not include historic data.
     * @param {String} id The id of the process definition.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.failedJobs Whether to include the number of failed jobs in the result or not. Valid values are &#x60;true&#x60; or &#x60;false&#x60;.
     * @param {Boolean} opts.incidents Valid values for this property are &#x60;true&#x60; or &#x60;false&#x60;. If this property has been set to &#x60;true&#x60; the result will include the corresponding number of incidents for each occurred incident type. If it is set to &#x60;false&#x60;, the incidents will not be included in the result. Cannot be used in combination with &#x60;incidentsForType&#x60;.
     * @param {String} opts.incidentsForType If this property has been set with any incident type (i.e., a string value) the result will only include the number of incidents for the assigned incident type. Cannot be used in combination with &#x60;incidents&#x60;. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {module:api/ProcessDefinitionApi~getActivityStatisticsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getActivityStatistics(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getActivityStatistics");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'failedJobs': opts['failedJobs'],'incidents': opts['incidents'],'incidentsForType': opts['incidentsForType']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [ActivityStatisticsResultDto];

      return this.apiClient.callApi(
        '/process-definition/{id}/statistics', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getActivityStatisticsByProcessDefinitionKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getActivityStatisticsByProcessDefinitionKeyCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ActivityStatisticsResultDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Activity Instance Statistics
     * Retrieves runtime statistics of the latest version of the given process definition which belongs to no tenant, grouped by activities. These statistics include the number of running activity instances, optionally the number of failed jobs and also optionally the number of incidents either grouped by incident types or for a specific incident type. **Note**: This does not include historic data.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.failedJobs Whether to include the number of failed jobs in the result or not. Valid values are &#x60;true&#x60; or &#x60;false&#x60;.
     * @param {Boolean} opts.incidents Valid values for this property are &#x60;true&#x60; or &#x60;false&#x60;. If this property has been set to &#x60;true&#x60; the result will include the corresponding number of incidents for each occurred incident type. If it is set to &#x60;false&#x60;, the incidents will not be included in the result. Cannot be used in combination with &#x60;incidentsForType&#x60;.
     * @param {String} opts.incidentsForType If this property has been set with any incident type (i.e., a string value) the result will only include the number of incidents for the assigned incident type. Cannot be used in combination with &#x60;incidents&#x60;. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {module:api/ProcessDefinitionApi~getActivityStatisticsByProcessDefinitionKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getActivityStatisticsByProcessDefinitionKey(key, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getActivityStatisticsByProcessDefinitionKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        'failedJobs': opts['failedJobs'],'incidents': opts['incidents'],'incidentsForType': opts['incidentsForType']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [ActivityStatisticsResultDto];

      return this.apiClient.callApi(
        '/process-definition/key/{key}/statistics', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getActivityStatisticsByProcessDefinitionKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getActivityStatisticsByProcessDefinitionKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ActivityStatisticsResultDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Activity Instance Statistics
     * Retrieves runtime statistics of the latest version of the given process definition for a tenant, grouped by activities. These statistics include the number of running activity instances, optionally the number of failed jobs and also optionally the number of incidents either grouped by incident types or for a specific incident type. **Note**: This does not include historic data.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.failedJobs Whether to include the number of failed jobs in the result or not. Valid values are &#x60;true&#x60; or &#x60;false&#x60;.
     * @param {Boolean} opts.incidents Valid values for this property are &#x60;true&#x60; or &#x60;false&#x60;. If this property has been set to &#x60;true&#x60; the result will include the corresponding number of incidents for each occurred incident type. If it is set to &#x60;false&#x60;, the incidents will not be included in the result. Cannot be used in combination with &#x60;incidentsForType&#x60;.
     * @param {String} opts.incidentsForType If this property has been set with any incident type (i.e., a string value) the result will only include the number of incidents for the assigned incident type. Cannot be used in combination with &#x60;incidents&#x60;. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {module:api/ProcessDefinitionApi~getActivityStatisticsByProcessDefinitionKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getActivityStatisticsByProcessDefinitionKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getActivityStatisticsByProcessDefinitionKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getActivityStatisticsByProcessDefinitionKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        'failedJobs': opts['failedJobs'],'incidents': opts['incidents'],'incidentsForType': opts['incidentsForType']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [ActivityStatisticsResultDto];

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/statistics', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getDeployedStartForm operation.
     * @callback moduleapi/ProcessDefinitionApi~getDeployedStartFormCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Deployed Start Form
     * Retrieves the deployed form that can be referenced from a start event. For further information please refer to [User Guide](https://docs.camunda.org/manual/develop/user-guide/task-forms/#embedded-task-forms).
     * @param {String} id The id of the process definition to get the deployed start form for.
     * @param {module:api/ProcessDefinitionApi~getDeployedStartFormCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getDeployedStartForm(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getDeployedStartForm");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/{id}/deployed-start-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getDeployedStartFormByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getDeployedStartFormByKeyCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Deployed Start Form
     * Retrieves the deployed form that can be referenced from a start event. For further information please refer to [User Guide](https://docs.camunda.org/manual/develop/user-guide/task-forms/#embedded-task-forms).
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {module:api/ProcessDefinitionApi~getDeployedStartFormByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getDeployedStartFormByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getDeployedStartFormByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/deployed-start-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getDeployedStartFormByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getDeployedStartFormByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Deployed Start Form
     * Retrieves the deployed form that can be referenced from a start event. For further information please refer to [User Guide](https://docs.camunda.org/manual/develop/user-guide/task-forms/#embedded-task-forms).
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definitions belong to.
     * @param {module:api/ProcessDefinitionApi~getDeployedStartFormByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getDeployedStartFormByKeyAndTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getDeployedStartFormByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getDeployedStartFormByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/deployed-start-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getLatestProcessDefinitionByTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getLatestProcessDefinitionByTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get
     * Retrieves the latest version of the process definition for tenant according to the &#x60;ProcessDefinition&#x60; interface in the engine.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {module:api/ProcessDefinitionApi~getLatestProcessDefinitionByTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getLatestProcessDefinitionByTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getLatestProcessDefinitionByTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getLatestProcessDefinitionByTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinition operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get
     * Retrieves a process definition according to the &#x60;ProcessDefinition&#x60; interface in the engine.
     * @param {String} id The id of the process definition to be retrieved.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinition(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getProcessDefinition");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDto;

      return this.apiClient.callApi(
        '/process-definition/{id}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionBpmn20Xml operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDiagramDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get XML
     * Retrieves the BPMN 2.0 XML of a process definition.
     * @param {String} id The id of the process definition.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionBpmn20Xml(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getProcessDefinitionBpmn20Xml");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDiagramDto;

      return this.apiClient.callApi(
        '/process-definition/{id}/xml', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionBpmn20XmlByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlByKeyCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDiagramDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get XML
     * Retrieves latest version the BPMN 2.0 XML of a process definition.
     * @param {String} key The key of the process definition (the latest version thereof) whose XML should be retrieved.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionBpmn20XmlByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getProcessDefinitionBpmn20XmlByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDiagramDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/xml', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionBpmn20XmlByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDiagramDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get XML
     * Retrieves latest version the BPMN 2.0 XML of a process definition. Returns the XML for the latest version of the process definition for tenant.
     * @param {String} key The key of the process definition (the latest version thereof) whose XML should be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionBpmn20XmlByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionBpmn20XmlByKeyAndTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getProcessDefinitionBpmn20XmlByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getProcessDefinitionBpmn20XmlByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDiagramDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/xml', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionByKeyCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessDefinitionDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get
     * Retrieves the latest version of the process definition which belongs to no tenant according to the &#x60;ProcessDefinition&#x60; interface in the engine.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getProcessDefinitionByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = ProcessDefinitionDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionDiagram operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionDiagramCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Diagram
     * Retrieves the diagram of a process definition.  If the process definition&#x27;s deployment contains an image resource with the same file name as the process definition, the deployed image will be returned by the Get Diagram endpoint. Example: &#x60;someProcess.bpmn&#x60; and &#x60;someProcess.png&#x60;. Supported file extentions for the image are: &#x60;svg&#x60;, &#x60;png&#x60;, &#x60;jpg&#x60;, and &#x60;gif&#x60;.
     * @param {String} id The id of the process definition.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionDiagramCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionDiagram(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getProcessDefinitionDiagram");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/octet-stream', '*/*', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/{id}/diagram', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionDiagramByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionDiagramByKeyCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Diagram
     * Retrieves the diagram for the latest version of the process definition which belongs to no tenant.  If the process definition&#x27;s deployment contains an image resource with the same file name as the process definition, the deployed image will be returned by the Get Diagram endpoint. Example: &#x60;someProcess.bpmn&#x60; and &#x60;someProcess.png&#x60;. Supported file extentions for the image are: &#x60;svg&#x60;, &#x60;png&#x60;, &#x60;jpg&#x60;, and &#x60;gif&#x60;.
     * @param {String} key The key of the process definition.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionDiagramByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionDiagramByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getProcessDefinitionDiagramByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/octet-stream', '*/*', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/diagram', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionDiagramByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionDiagramByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Diagram
     * Retrieves the diagram for the latest version of the process definition for tenant.  If the process definition&#x27;s deployment contains an image resource with the same file name as the process definition, the deployed image will be returned by the Get Diagram endpoint. Example: &#x60;someProcess.bpmn&#x60; and &#x60;someProcess.png&#x60;. Supported file extentions for the image are: &#x60;svg&#x60;, &#x60;png&#x60;, &#x60;jpg&#x60;, and &#x60;gif&#x60;.
     * @param {String} key The key of the process definition.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionDiagramByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionDiagramByKeyAndTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getProcessDefinitionDiagramByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getProcessDefinitionDiagramByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/octet-stream', '*/*', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/diagram', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionStatistics operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionStatisticsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ProcessDefinitionStatisticsResultDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Process Instance Statistics
     * Retrieves runtime statistics of the process engine, grouped by process definitions. These statistics include the number of running process instances, optionally the number of failed jobs and also optionally the number of incidents either grouped by incident types or for a specific incident type. **Note**: This does not include historic data.
     * @param {Object} opts Optional parameters
     * @param {Boolean} opts.failedJobs Whether to include the number of failed jobs in the result or not. Valid values are &#x60;true&#x60; or &#x60;false&#x60;.
     * @param {Boolean} opts.incidents Valid values for this property are &#x60;true&#x60; or &#x60;false&#x60;. If this property has been set to &#x60;true&#x60; the result will include the corresponding number of incidents for each occurred incident type. If it is set to &#x60;false&#x60;, the incidents will not be included in the result. Cannot be used in combination with &#x60;incidentsForType&#x60;.
     * @param {String} opts.incidentsForType If this property has been set with any incident type (i.e., a string value) the result will only include the number of incidents for the assigned incident type. Cannot be used in combination with &#x60;incidents&#x60;. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {Boolean} opts.rootIncidents Valid values for this property are &#x60;true&#x60; or &#x60;false&#x60;. If this property has been set to &#x60;true&#x60; the result will include the corresponding number of root incidents for each occurred incident type. If it is set to &#x60;false&#x60;, the incidents will not be included in the result. Cannot be used in combination with &#x60;incidentsForType&#x60; or &#x60;incidents&#x60;.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionStatisticsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionStatistics(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
        
      };
      let queryParams = {
        'failedJobs': opts['failedJobs'],'incidents': opts['incidents'],'incidentsForType': opts['incidentsForType'],'rootIncidents': opts['rootIncidents']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [ProcessDefinitionStatisticsResultDto];

      return this.apiClient.callApi(
        '/process-definition/statistics', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitions operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/ProcessDefinitionDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get List
     * Queries for process definitions that fulfill given parameters. Parameters may be the properties of  process definitions, such as the name, key or version. The size of the result set can be retrieved by using the [Get Definition Count](https://docs.camunda.org/manual/develop/reference/rest/process-definition/get-query-count/) method.
     * @param {Object} opts Optional parameters
     * @param {String} opts.processDefinitionId Filter by process definition id.
     * @param {String} opts.processDefinitionIdIn Filter by a comma-separated list of process definition ids.
     * @param {String} opts.name Filter by process definition name.
     * @param {String} opts.nameLike Filter by process definition names that the parameter is a substring of.
     * @param {String} opts.deploymentId Filter by the deployment the id belongs to.
     * @param {Date} opts.deployedAfter Filter by the deploy time of the deployment the process definition belongs to. Only selects process definitions that have been deployed after (exclusive) a specific time. By [default](https://docs.camunda.org/manual/develop/reference/rest/overview/date-format/), the date must have the format &#x60;yyyy-MM-dd&#x27;T&#x27;HH:mm:ss.SSSZ&#x60;, e.g., &#x60;2013-01-23T14:42:45.546+0200&#x60;.
     * @param {Date} opts.deployedAt Filter by the deploy time of the deployment the process definition belongs to. Only selects process definitions that have been deployed at a specific time (exact match). By [default](https://docs.camunda.org/manual/develop/reference/rest/overview/date-format/), the date must have the format &#x60;yyyy-MM-dd&#x27;T&#x27;HH:mm:ss.SSSZ&#x60;, e.g., &#x60;2013-01-23T14:42:45.546+0200&#x60;.
     * @param {String} opts.key Filter by process definition key, i.e., the id in the BPMN 2.0 XML. Exact match.
     * @param {String} opts.keysIn Filter by a comma-separated list of process definition keys.
     * @param {String} opts.keyLike Filter by process definition keys that the parameter is a substring of.
     * @param {String} opts.category Filter by process definition category. Exact match.
     * @param {String} opts.categoryLike Filter by process definition categories that the parameter is a substring of.
     * @param {Number} opts.version Filter by process definition version.
     * @param {Boolean} opts.latestVersion Only include those process definitions that are latest versions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.resourceName Filter by the name of the process definition resource. Exact match.
     * @param {String} opts.resourceNameLike Filter by names of those process definition resources that the parameter is a substring of.
     * @param {String} opts.startableBy Filter by a user name who is allowed to start the process.
     * @param {Boolean} opts.active Only include active process definitions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {Boolean} opts.suspended Only include suspended process definitions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.incidentId Filter by the incident id.
     * @param {String} opts.incidentType Filter by the incident type. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {String} opts.incidentMessage Filter by the incident message. Exact match.
     * @param {String} opts.incidentMessageLike Filter by the incident message that the parameter is a substring of.
     * @param {String} opts.tenantIdIn Filter by a comma-separated list of tenant ids. A process definition must have one of the given tenant ids.
     * @param {Boolean} opts.withoutTenantId Only include process definitions which belong to no tenant. Value may only be true, as false is the default behavior.
     * @param {Boolean} opts.includeProcessDefinitionsWithoutTenantId Include process definitions which belong to no tenant. Can be used in combination with &#x60;tenantIdIn&#x60;. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.versionTag Filter by the version tag.
     * @param {String} opts.versionTagLike Filter by the version tag that the parameter is a substring of.
     * @param {Boolean} opts.withoutVersionTag Only include process definitions without a &#x60;versionTag&#x60;.
     * @param {Boolean} opts.startableInTasklist Filter by process definitions which are startable in Tasklist..
     * @param {Boolean} opts.notStartableInTasklist Filter by process definitions which are not startable in Tasklist.
     * @param {Boolean} opts.startablePermissionCheck Filter by process definitions which the user is allowed to start in Tasklist. If the user doesn&#x27;t have these permissions the result will be empty list. The permissions are: * &#x60;CREATE&#x60; permission for all Process instances * &#x60;CREATE_INSTANCE&#x60; and &#x60;READ&#x60; permission on Process definition level
     * @param {module:model/String} opts.sortBy Sort the results lexicographically by a given criterion. Must be used in conjunction with the sortOrder parameter.
     * @param {module:model/String} opts.sortOrder Sort the results in a given order. Values may be asc for ascending order or desc for descending order. Must be used in conjunction with the sortBy parameter.
     * @param {Number} opts.firstResult Pagination of results. Specifies the index of the first result to return.
     * @param {Number} opts.maxResults Pagination of results. Specifies the maximum number of results to return. Will return less results if there are no more results left.
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitions(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
        
      };
      let queryParams = {
        'processDefinitionId': opts['processDefinitionId'],'processDefinitionIdIn': opts['processDefinitionIdIn'],'name': opts['name'],'nameLike': opts['nameLike'],'deploymentId': opts['deploymentId'],'deployedAfter': opts['deployedAfter'],'deployedAt': opts['deployedAt'],'key': opts['key'],'keysIn': opts['keysIn'],'keyLike': opts['keyLike'],'category': opts['category'],'categoryLike': opts['categoryLike'],'version': opts['version'],'latestVersion': opts['latestVersion'],'resourceName': opts['resourceName'],'resourceNameLike': opts['resourceNameLike'],'startableBy': opts['startableBy'],'active': opts['active'],'suspended': opts['suspended'],'incidentId': opts['incidentId'],'incidentType': opts['incidentType'],'incidentMessage': opts['incidentMessage'],'incidentMessageLike': opts['incidentMessageLike'],'tenantIdIn': opts['tenantIdIn'],'withoutTenantId': opts['withoutTenantId'],'includeProcessDefinitionsWithoutTenantId': opts['includeProcessDefinitionsWithoutTenantId'],'versionTag': opts['versionTag'],'versionTagLike': opts['versionTagLike'],'withoutVersionTag': opts['withoutVersionTag'],'startableInTasklist': opts['startableInTasklist'],'notStartableInTasklist': opts['notStartableInTasklist'],'startablePermissionCheck': opts['startablePermissionCheck'],'sortBy': opts['sortBy'],'sortOrder': opts['sortOrder'],'firstResult': opts['firstResult'],'maxResults': opts['maxResults']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [ProcessDefinitionDto];

      return this.apiClient.callApi(
        '/process-definition', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getProcessDefinitionsCount operation.
     * @callback moduleapi/ProcessDefinitionApi~getProcessDefinitionsCountCallback
     * @param {String} error Error message, if any.
     * @param {module:model/CountResultDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get List Count
     * Requests the number of process definitions that fulfill the query criteria. Takes the same filtering parameters as the [Get Definitions](https://docs.camunda.org/manual/develop/reference/rest/process-definition/get-query/) method.
     * @param {Object} opts Optional parameters
     * @param {String} opts.processDefinitionId Filter by process definition id.
     * @param {String} opts.processDefinitionIdIn Filter by a comma-separated list of process definition ids.
     * @param {String} opts.name Filter by process definition name.
     * @param {String} opts.nameLike Filter by process definition names that the parameter is a substring of.
     * @param {String} opts.deploymentId Filter by the deployment the id belongs to.
     * @param {Date} opts.deployedAfter Filter by the deploy time of the deployment the process definition belongs to. Only selects process definitions that have been deployed after (exclusive) a specific time. By [default](https://docs.camunda.org/manual/develop/reference/rest/overview/date-format/), the date must have the format &#x60;yyyy-MM-dd&#x27;T&#x27;HH:mm:ss.SSSZ&#x60;, e.g., &#x60;2013-01-23T14:42:45.546+0200&#x60;.
     * @param {Date} opts.deployedAt Filter by the deploy time of the deployment the process definition belongs to. Only selects process definitions that have been deployed at a specific time (exact match). By [default](https://docs.camunda.org/manual/develop/reference/rest/overview/date-format/), the date must have the format &#x60;yyyy-MM-dd&#x27;T&#x27;HH:mm:ss.SSSZ&#x60;, e.g., &#x60;2013-01-23T14:42:45.546+0200&#x60;.
     * @param {String} opts.key Filter by process definition key, i.e., the id in the BPMN 2.0 XML. Exact match.
     * @param {String} opts.keysIn Filter by a comma-separated list of process definition keys.
     * @param {String} opts.keyLike Filter by process definition keys that the parameter is a substring of.
     * @param {String} opts.category Filter by process definition category. Exact match.
     * @param {String} opts.categoryLike Filter by process definition categories that the parameter is a substring of.
     * @param {Number} opts.version Filter by process definition version.
     * @param {Boolean} opts.latestVersion Only include those process definitions that are latest versions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.resourceName Filter by the name of the process definition resource. Exact match.
     * @param {String} opts.resourceNameLike Filter by names of those process definition resources that the parameter is a substring of.
     * @param {String} opts.startableBy Filter by a user name who is allowed to start the process.
     * @param {Boolean} opts.active Only include active process definitions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {Boolean} opts.suspended Only include suspended process definitions. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.incidentId Filter by the incident id.
     * @param {String} opts.incidentType Filter by the incident type. See the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/incidents/#incident-types) for a list of incident types.
     * @param {String} opts.incidentMessage Filter by the incident message. Exact match.
     * @param {String} opts.incidentMessageLike Filter by the incident message that the parameter is a substring of.
     * @param {String} opts.tenantIdIn Filter by a comma-separated list of tenant ids. A process definition must have one of the given tenant ids.
     * @param {Boolean} opts.withoutTenantId Only include process definitions which belong to no tenant. Value may only be true, as false is the default behavior.
     * @param {Boolean} opts.includeProcessDefinitionsWithoutTenantId Include process definitions which belong to no tenant. Can be used in combination with &#x60;tenantIdIn&#x60;. Value may only be &#x60;true&#x60;, as &#x60;false&#x60; is the default behavior.
     * @param {String} opts.versionTag Filter by the version tag.
     * @param {String} opts.versionTagLike Filter by the version tag that the parameter is a substring of.
     * @param {Boolean} opts.withoutVersionTag Only include process definitions without a &#x60;versionTag&#x60;.
     * @param {Boolean} opts.startableInTasklist Filter by process definitions which are startable in Tasklist..
     * @param {Boolean} opts.notStartableInTasklist Filter by process definitions which are not startable in Tasklist.
     * @param {Boolean} opts.startablePermissionCheck Filter by process definitions which the user is allowed to start in Tasklist. If the user doesn&#x27;t have these permissions the result will be empty list. The permissions are: * &#x60;CREATE&#x60; permission for all Process instances * &#x60;CREATE_INSTANCE&#x60; and &#x60;READ&#x60; permission on Process definition level
     * @param {module:api/ProcessDefinitionApi~getProcessDefinitionsCountCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getProcessDefinitionsCount(opts, callback) {
      opts = opts || {};
      let postBody = null;

      let pathParams = {
        
      };
      let queryParams = {
        'processDefinitionId': opts['processDefinitionId'],'processDefinitionIdIn': opts['processDefinitionIdIn'],'name': opts['name'],'nameLike': opts['nameLike'],'deploymentId': opts['deploymentId'],'deployedAfter': opts['deployedAfter'],'deployedAt': opts['deployedAt'],'key': opts['key'],'keysIn': opts['keysIn'],'keyLike': opts['keyLike'],'category': opts['category'],'categoryLike': opts['categoryLike'],'version': opts['version'],'latestVersion': opts['latestVersion'],'resourceName': opts['resourceName'],'resourceNameLike': opts['resourceNameLike'],'startableBy': opts['startableBy'],'active': opts['active'],'suspended': opts['suspended'],'incidentId': opts['incidentId'],'incidentType': opts['incidentType'],'incidentMessage': opts['incidentMessage'],'incidentMessageLike': opts['incidentMessageLike'],'tenantIdIn': opts['tenantIdIn'],'withoutTenantId': opts['withoutTenantId'],'includeProcessDefinitionsWithoutTenantId': opts['includeProcessDefinitionsWithoutTenantId'],'versionTag': opts['versionTag'],'versionTagLike': opts['versionTagLike'],'withoutVersionTag': opts['withoutVersionTag'],'startableInTasklist': opts['startableInTasklist'],'notStartableInTasklist': opts['notStartableInTasklist'],'startablePermissionCheck': opts['startablePermissionCheck']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = CountResultDto;

      return this.apiClient.callApi(
        '/process-definition/count', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getRenderedStartForm operation.
     * @callback moduleapi/ProcessDefinitionApi~getRenderedStartFormCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Rendered Start Form
     * Retrieves the rendered form for a process definition. This method can be used to get the HTML rendering of a [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} id The id of the process definition to get the rendered start form for.
     * @param {module:api/ProcessDefinitionApi~getRenderedStartFormCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getRenderedStartForm(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getRenderedStartForm");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/{id}/rendered-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getRenderedStartFormByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getRenderedStartFormByKeyCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Rendered Start Form
     * Retrieves  the rendered form for the latest version of the process definition which belongs to no tenant. This method can be used to get the HTML rendering of a [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {module:api/ProcessDefinitionApi~getRenderedStartFormByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getRenderedStartFormByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getRenderedStartFormByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/rendered-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getRenderedStartFormByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getRenderedStartFormByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {'Blob'{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Rendered Start Form
     * Retrieves  the rendered form for the latest version of the process definition for a tenant. This method can be used to get the HTML rendering of a [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {module:api/ProcessDefinitionApi~getRenderedStartFormByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getRenderedStartFormByKeyAndTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getRenderedStartFormByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getRenderedStartFormByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/xhtml+xml', 'application/json'];
      let returnType = 'Blob';

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/rendered-form', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartForm operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormCallback
     * @param {String} error Error message, if any.
     * @param {module:model/FormDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Key
     * Retrieves the key of the start form for a process definition. The form key corresponds to the &#x60;FormData#formKey&#x60; property in the engine.
     * @param {String} id The id of the process definition to get the start form key for.
     * @param {module:api/ProcessDefinitionApi~getStartFormCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartForm(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getStartForm");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = FormDto;

      return this.apiClient.callApi(
        '/process-definition/{id}/startForm', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartFormByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormByKeyCallback
     * @param {String} error Error message, if any.
     * @param {module:model/FormDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Key
     * Retrieves the key of the start form for the latest version of the process definition which belongs to no tenant. The form key corresponds to the &#x60;FormData#formKey&#x60; property in the engine.
     * @param {String} key The key of the process definition (the latest version thereof) for which the form key is to be retrieved.
     * @param {module:api/ProcessDefinitionApi~getStartFormByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartFormByKey(key, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getStartFormByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = FormDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/startForm', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartFormByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {module:model/FormDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Key
     * Retrieves the key of the start form for the latest version of the process definition for a tenant. The form key corresponds to the &#x60;FormData#formKey&#x60; property in the engine.
     * @param {String} key The key of the process definition (the latest version thereof) for which the form key is to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {module:api/ProcessDefinitionApi~getStartFormByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartFormByKeyAndTenantId(key, tenantId, callback) {
      
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getStartFormByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getStartFormByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = FormDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/startForm', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartFormVariables operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormVariablesCallback
     * @param {String} error Error message, if any.
     * @param {Object.<String, module:model/{'String': VariableValueDto}>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Variables
     * Retrieves the start form variables for a process definition (only if they are defined via the  [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms) approach). The start form variables take form data specified on the start event into account. If form fields are defined, the variable types and default values of the form fields are taken into account.
     * @param {String} id The id of the process definition to retrieve the variables for.
     * @param {Object} opts Optional parameters
     * @param {String} opts.variableNames A comma-separated list of variable names. Allows restricting the list of requested variables to the variable names in the list. It is best practice to restrict the list of variables to the variables actually required by the form in order to minimize fetching of data. If the query parameter is ommitted all variables are fetched. If the query parameter contains non-existent variable names, the variable names are ignored.
     * @param {Boolean} opts.deserializeValues Determines whether serializable variable values (typically variables that store custom Java objects) should be deserialized on server side (default true).  If set to true, a serializable variable will be deserialized on server side and transformed to JSON using [Jackson&#x27;s](http://jackson.codehaus.org/) POJO/bean property introspection feature. Note that this requires the Java classes of the variable value to be on the REST API&#x27;s classpath.  If set to false, a serializable variable will be returned in its serialized format. For example, a variable that is serialized as XML will be returned as a JSON string containing XML.  **Note**: While true is the default value for reasons of backward compatibility, we recommend setting this parameter to false when developing web applications that are independent of the Java process applications deployed to the engine. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~getStartFormVariablesCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartFormVariables(id, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getStartFormVariables");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        'variableNames': opts['variableNames'],'deserializeValues': opts['deserializeValues']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = {'String': VariableValueDto};

      return this.apiClient.callApi(
        '/process-definition/{id}/form-variables', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartFormVariablesByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormVariablesByKeyCallback
     * @param {String} error Error message, if any.
     * @param {Object.<String, module:model/{'String': VariableValueDto}>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Variables
     * Retrieves the start form variables for the latest process definition which belongs to no tenant (only if they are defined via the  [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms) approach). The start form variables take form data specified on the start event into account. If form fields are defined, the variable types and default values of the form fields are taken into account.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {Object} opts Optional parameters
     * @param {String} opts.variableNames A comma-separated list of variable names. Allows restricting the list of requested variables to the variable names in the list. It is best practice to restrict the list of variables to the variables actually required by the form in order to minimize fetching of data. If the query parameter is ommitted all variables are fetched. If the query parameter contains non-existent variable names, the variable names are ignored.
     * @param {Boolean} opts.deserializeValues Determines whether serializable variable values (typically variables that store custom Java objects) should be deserialized on server side (default true).  If set to true, a serializable variable will be deserialized on server side and transformed to JSON using [Jackson&#x27;s](http://jackson.codehaus.org/) POJO/bean property introspection feature. Note that this requires the Java classes of the variable value to be on the REST API&#x27;s classpath.  If set to false, a serializable variable will be returned in its serialized format. For example, a variable that is serialized as XML will be returned as a JSON string containing XML.  **Note**: While true is the default value for reasons of backward compatibility, we recommend setting this parameter to false when developing web applications that are independent of the Java process applications deployed to the engine. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~getStartFormVariablesByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartFormVariablesByKey(key, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getStartFormVariablesByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        'variableNames': opts['variableNames'],'deserializeValues': opts['deserializeValues']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = {'String': VariableValueDto};

      return this.apiClient.callApi(
        '/process-definition/key/{key}/form-variables', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStartFormVariablesByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~getStartFormVariablesByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {Object.<String, module:model/{'String': VariableValueDto}>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Start Form Variables
     * Retrieves the start form variables for the latest process definition for a tenant (only if they are defined via the  [Generated Task Form](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms) approach). The start form variables take form data specified on the start event into account. If form fields are defined, the variable types and default values of the form fields are taken into account.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {String} opts.variableNames A comma-separated list of variable names. Allows restricting the list of requested variables to the variable names in the list. It is best practice to restrict the list of variables to the variables actually required by the form in order to minimize fetching of data. If the query parameter is ommitted all variables are fetched. If the query parameter contains non-existent variable names, the variable names are ignored.
     * @param {Boolean} opts.deserializeValues Determines whether serializable variable values (typically variables that store custom Java objects) should be deserialized on server side (default true).  If set to true, a serializable variable will be deserialized on server side and transformed to JSON using [Jackson&#x27;s](http://jackson.codehaus.org/) POJO/bean property introspection feature. Note that this requires the Java classes of the variable value to be on the REST API&#x27;s classpath.  If set to false, a serializable variable will be returned in its serialized format. For example, a variable that is serialized as XML will be returned as a JSON string containing XML.  **Note**: While true is the default value for reasons of backward compatibility, we recommend setting this parameter to false when developing web applications that are independent of the Java process applications deployed to the engine. (default to <.>)
     * @param {module:api/ProcessDefinitionApi~getStartFormVariablesByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStartFormVariablesByKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = null;
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling getStartFormVariablesByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling getStartFormVariablesByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        'variableNames': opts['variableNames'],'deserializeValues': opts['deserializeValues']
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = {'String': VariableValueDto};

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/form-variables', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the getStaticCalledProcessDefinitions operation.
     * @callback moduleapi/ProcessDefinitionApi~getStaticCalledProcessDefinitionsCallback
     * @param {String} error Error message, if any.
     * @param {Array.<module:model/CalledProcessDefinitionDto>{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Get Static Called Process Definitions
     * For the given process, returns a list of called process definitions corresponding to the &#x60;CalledProcessDefinition&#x60; interface in the engine. The list contains all process definitions that are referenced statically by call activities in the given process. This endpoint does not resolve process definitions that are referenced with expressions. Each called process definition contains a list of call activity ids, which specifies the call activities that are calling that process. This endpoint does not resolve references to case definitions.
     * @param {String} id The id of the process definition.
     * @param {module:api/ProcessDefinitionApi~getStaticCalledProcessDefinitionsCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    getStaticCalledProcessDefinitions(id, callback) {
      
      let postBody = null;
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling getStaticCalledProcessDefinitions");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = [];
      let accepts = ['application/json'];
      let returnType = [CalledProcessDefinitionDto];

      return this.apiClient.callApi(
        '/process-definition/{id}/static-called-process-definitions', 'GET',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the restartProcessInstance operation.
     * @callback moduleapi/ProcessDefinitionApi~restartProcessInstanceCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Restart Process Instance
     * Restarts process instances that were canceled or terminated synchronously. Can also restart completed process instances. It will create a new instance using the original instance information. To execute the restart asynchronously, use the [Restart Process Instance Async](https://docs.camunda.org/manual/develop/reference/rest/process-definition/post-restart-process-instance-async/) method.  For more information about the difference between synchronous and asynchronous execution, please refer to the related section of the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/process-instance-restart/#execution).
     * @param {String} id The id of the process definition of the process instances to restart.
     * @param {Object} opts Optional parameters
     * @param {module:model/RestartProcessInstanceDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~restartProcessInstanceCallback} callback The callback function, accepting three arguments: error, data, response
     */
    restartProcessInstance(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling restartProcessInstance");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/{id}/restart', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the restartProcessInstanceAsyncOperation operation.
     * @callback moduleapi/ProcessDefinitionApi~restartProcessInstanceAsyncOperationCallback
     * @param {String} error Error message, if any.
     * @param {module:model/BatchDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Restart Process Instance Async
     * Restarts process instances that were canceled or terminated asynchronously. Can also restart completed process instances. It will create a new instance using the original instance information. To execute the restart asynchronously, use the [Restart Process Instance](https://docs.camunda.org/manual/develop/reference/rest/process-definition/post-restart-process-instance-sync/) method.  For more information about the difference between synchronous and asynchronous execution, please refer to the related section of the [User Guide](https://docs.camunda.org/manual/develop/user-guide/process-engine/process-instance-restart/#execution).
     * @param {String} id The id of the process definition of the process instances to restart.
     * @param {Object} opts Optional parameters
     * @param {module:model/RestartProcessInstanceDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~restartProcessInstanceAsyncOperationCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    restartProcessInstanceAsyncOperation(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling restartProcessInstanceAsyncOperation");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = BatchDto;

      return this.apiClient.callApi(
        '/process-definition/{id}/restart-async', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the startProcessInstance operation.
     * @callback moduleapi/ProcessDefinitionApi~startProcessInstanceCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceWithVariablesDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Start Instance
     * Instantiates a given process definition. Process variables and business key may be supplied in the request body.
     * @param {String} id The id of the process definition to be retrieved.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~startProcessInstanceCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    startProcessInstance(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling startProcessInstance");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceWithVariablesDto;

      return this.apiClient.callApi(
        '/process-definition/{id}/start', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the startProcessInstanceByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~startProcessInstanceByKeyCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceWithVariablesDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Start Instance
     * Instantiates a given process definition, starts the latest version of the process definition which belongs to no tenant. Process variables and business key may be supplied in the request body.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~startProcessInstanceByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    startProcessInstanceByKey(key, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling startProcessInstanceByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceWithVariablesDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/start', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the startProcessInstanceByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~startProcessInstanceByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceWithVariablesDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Start Instance
     * Instantiates a given process definition, starts the latest version of the process definition for tenant. Process variables and business key may be supplied in the request body.
     * @param {String} key The key of the process definition (the latest version thereof) to be retrieved.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~startProcessInstanceByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    startProcessInstanceByKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling startProcessInstanceByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling startProcessInstanceByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceWithVariablesDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/start', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the submitForm operation.
     * @callback moduleapi/ProcessDefinitionApi~submitFormCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Submit Start Form
     * Starts a process instance using a set of process variables and the business key. If the start event has Form Field Metadata defined, the process engine will perform backend validation for any form fields which have validators defined. See [Documentation on Generated Task Forms](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} id The id of the process definition to submit the form for.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceFormDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~submitFormCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    submitForm(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling submitForm");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceDto;

      return this.apiClient.callApi(
        '/process-definition/{id}/submit-form', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the submitFormByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~submitFormByKeyCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Submit Start Form
     * Starts the latest version of the process definition which belongs to no tenant using a set of process variables and the business key. If the start event has Form Field Metadata defined, the process engine will perform backend validation for any form fields which have validators defined. See [Documentation on Generated Task Forms](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} key The key of the process definition to submit the form for.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceFormDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~submitFormByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    submitFormByKey(key, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling submitFormByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/submit-form', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the submitFormByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~submitFormByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param {module:model/ProcessInstanceDto{ data The data returned by the service call.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Submit Start Form
     * Starts the latest version of the process definition for a tenant using a set of process variables and the business key. If the start event has Form Field Metadata defined, the process engine will perform backend validation for any form fields which have validators defined. See [Documentation on Generated Task Forms](https://docs.camunda.org/manual/develop/user-guide/task-forms/#generated-task-forms).
     * @param {String} key The key of the process definition to submit the form for.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {module:model/StartProcessInstanceFormDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~submitFormByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     * data is of type: {@link <&vendorExtensions.x-jsdoc-type>}
     */
    submitFormByKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling submitFormByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling submitFormByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = ProcessInstanceDto;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/submit-form', 'POST',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateHistoryTimeToLiveByProcessDefinitionId operation.
     * @callback moduleapi/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionIdCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update History Time to Live
     * Updates history time to live for process definition. The field is used within [History cleanup](https://docs.camunda.org/manual/develop/user-guide/process-engine/history/#history-cleanup).
     * @param {String} id The id of the process definition to change history time to live.
     * @param {Object} opts Optional parameters
     * @param {module:model/HistoryTimeToLiveDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionIdCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateHistoryTimeToLiveByProcessDefinitionId(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling updateHistoryTimeToLiveByProcessDefinitionId");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/{id}/history-time-to-live', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateHistoryTimeToLiveByProcessDefinitionKey operation.
     * @callback moduleapi/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionKeyCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update History Time to Live
     * Updates history time to live for the latest version of the process definition which belongs to no tenant. The field is used within [History cleanup](https://docs.camunda.org/manual/develop/user-guide/process-engine/history/#history-cleanup).
     * @param {String} key The key of the process definition to change history time to live.
     * @param {Object} opts Optional parameters
     * @param {module:model/HistoryTimeToLiveDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionKeyCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateHistoryTimeToLiveByProcessDefinitionKey(key, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling updateHistoryTimeToLiveByProcessDefinitionKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/history-time-to-live', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Update History Time to Live
     * Updates history time to live for the latest version of the process definition for a tenant. The field is used within [History cleanup](https://docs.camunda.org/manual/develop/user-guide/process-engine/history/#history-cleanup).
     * @param {String} key The key of the process definition to change history time to live.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {module:model/HistoryTimeToLiveDto} opts.body 
     * @param {module:api/ProcessDefinitionApi~updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling updateHistoryTimeToLiveByProcessDefinitionKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/history-time-to-live', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateProcessDefinitionSuspensionState operation.
     * @callback moduleapi/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Activate/Suspend By Key
     * Activates or suspends process definitions with the given process definition key.
     * @param {Object} opts Optional parameters
     * @param {module:model/ProcessDefinitionSuspensionStateDto} opts.body **Note**: Unallowed property is &#x60;processDefinitionId&#x60;.
     * @param {module:api/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateProcessDefinitionSuspensionState(opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];

      let pathParams = {
        
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/suspended', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateProcessDefinitionSuspensionStateById operation.
     * @callback moduleapi/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByIdCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Activate/Suspend By Id
     * Activates or suspends a given process definition by id.
     * @param {String} id The id of the process definition to activate or suspend.
     * @param {Object} opts Optional parameters
     * @param {module:model/ProcessDefinitionSuspensionStateDto} opts.body **Note**: Unallowed properties are &#x60;processDefinitionId&#x60; and &#x60;processDefinitionKey&#x60;.
     * @param {module:api/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByIdCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateProcessDefinitionSuspensionStateById(id, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'id' is set
      if (id === undefined || id === null) {
        throw new Error("Missing the required parameter 'id' when calling updateProcessDefinitionSuspensionStateById");
      }

      let pathParams = {
        'id': id
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/{id}/suspended', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateProcessDefinitionSuspensionStateByKey operation.
     * @callback moduleapi/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByKeyCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Activate/Suspend by Id
     * Activates or suspends a given process definition by latest version of process definition key which belongs to no tenant.
     * @param {String} key The key of the process definition (the latest version thereof) to be activated/suspended.
     * @param {Object} opts Optional parameters
     * @param {module:model/ProcessDefinitionSuspensionStateDto} opts.body **Note**: Unallowed properties are &#x60;processDefinitionId&#x60; and &#x60;processDefinitionKey&#x60;.
     * @param {module:api/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByKeyCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateProcessDefinitionSuspensionStateByKey(key, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling updateProcessDefinitionSuspensionStateByKey");
      }

      let pathParams = {
        'key': key
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/suspended', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }
    /**
     * Callback function to receive the result of the updateProcessDefinitionSuspensionStateByKeyAndTenantId operation.
     * @callback moduleapi/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByKeyAndTenantIdCallback
     * @param {String} error Error message, if any.
     * @param data This operation does not return a value.
     * @param {String} response The complete HTTP response.
     */

    /**
     * Activate/Suspend by Id
     * Activates or suspends a given process definition by the latest version of the process definition for tenant.
     * @param {String} key The key of the process definition (the latest version thereof) to be activated/suspended.
     * @param {String} tenantId The id of the tenant the process definition belongs to.
     * @param {Object} opts Optional parameters
     * @param {module:model/ProcessDefinitionSuspensionStateDto} opts.body **Note**: Unallowed properties are &#x60;processDefinitionId&#x60; and &#x60;processDefinitionKey&#x60;.
     * @param {module:api/ProcessDefinitionApi~updateProcessDefinitionSuspensionStateByKeyAndTenantIdCallback} callback The callback function, accepting three arguments: error, data, response
     */
    updateProcessDefinitionSuspensionStateByKeyAndTenantId(key, tenantId, opts, callback) {
      opts = opts || {};
      let postBody = opts['body'];
      // verify the required parameter 'key' is set
      if (key === undefined || key === null) {
        throw new Error("Missing the required parameter 'key' when calling updateProcessDefinitionSuspensionStateByKeyAndTenantId");
      }
      // verify the required parameter 'tenantId' is set
      if (tenantId === undefined || tenantId === null) {
        throw new Error("Missing the required parameter 'tenantId' when calling updateProcessDefinitionSuspensionStateByKeyAndTenantId");
      }

      let pathParams = {
        'key': key,'tenant-id': tenantId
      };
      let queryParams = {
        
      };
      let headerParams = {
        
      };
      let formParams = {
        
      };

      let authNames = ['basicAuth'];
      let contentTypes = ['application/json'];
      let accepts = ['application/json'];
      let returnType = null;

      return this.apiClient.callApi(
        '/process-definition/key/{key}/tenant-id/{tenant-id}/suspended', 'PUT',
        pathParams, queryParams, headerParams, formParams, postBody,
        authNames, contentTypes, accepts, returnType, callback
      );
    }

}